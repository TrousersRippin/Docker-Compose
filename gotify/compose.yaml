---
services:
  gotify:
    image: docker.io/gotify/server:latest
    container_name: gotify
    hostname: gotify
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    env_file:
      - .env
    user: ${UID:-1000}:${GID:-1000}
    ports:
      - 8001:8001/tcp
    volumes:
      - $HOME/containers/storage/gotify/config:/app/data:rw
      - /etc/localtime:/etc/localtime:ro
    networks:
      - proxy
    labels:
      - logging=promtail
      - logging_jobname=containerlogs

networks:
  proxy:
    external: true