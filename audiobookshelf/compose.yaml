---
services:
  audiobookshelf:
    image: docker.io/advplyr/audiobookshelf:latest
    container_name: audiobookshelf
    hostname: audiobookshelf
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      - TZ=Europe/Stockholm
      - ACCESS_TOKEN_EXPIRY=604800
      - REFRESH_TOKEN_EXPIRY=2592000
    ports:
      - 13378:80/tcp
    volumes:
      - $HOME/containers/storage/audiobookshelf/config:/config:rw
      - $HOME/containers/storage/audiobookshelf/metadata:/metadata:rw
      - /mnt/hdd/audiobooks:/audiobooks:ro
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    networks:
      - proxy
    labels:
      - com.centurylinklabs.watchtower.enable=true
      - logging=promtail
      - logging_jobname=containerlogs

networks:
  proxy:
    external: true